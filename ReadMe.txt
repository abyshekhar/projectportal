- Installed Visual Studio Code
- Installed Node latest version
- Installed angular globally -- npm install -g @angular\cli @latest
- Installed dotnet 3.1.101
- Create a folder ProjectPortal and then create two subfolders inside it. One for the SPA and the other one for the API.(ProjectPortal-SPA and ProjectPortal-API respectively)
- Install bootstrap in the angular project using: npm install --save bootstrap

